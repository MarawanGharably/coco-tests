apiVersion: v1
kind: Service
metadata:
  name: {{ include "coco-cms-frontend.fullname" . }}
  labels:
    {{- include "coco-cms-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coco-cms-frontend.selectorLabels" . | nindent 4 }}